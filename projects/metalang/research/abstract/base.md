these are the base blocks in metalang!

- Program
- Transformer
- Wire, Connection, Route (Aliases for visible input/output routing)


```js
new Program("").wire().from("a").to("b").as('c")
Access("c").get("inputs")
```