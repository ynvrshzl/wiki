["dist/"](#)

This directory contains distrubuted, build files, used by other services and platforms, so it can be pre-packaged for any tool to inspect the wiki, without having to build their own schema, database, etc. Essentially, this directory is a distrubition framework layer.