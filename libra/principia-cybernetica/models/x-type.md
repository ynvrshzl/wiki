# "X-Type" (Matrix)
Today, the code is executing incongruent to it's expression.

```js
concat(1, 2, 3)
function concat(a, b, c){ return a + b + c; }
```

Separation of expression vs. execution vs. statement vs. arguments

```js
{
    function: "concat",
    args: [1, 2, 3],
    returns: [a, b, c]
}

```