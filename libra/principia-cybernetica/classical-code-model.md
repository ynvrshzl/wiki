# "Classical models of code"
Classical programming is __the source of the modern issues we face in engineering.__

## 1.0 - Classical Function Model
Code classicaly operates using __"micro-machines"__ ..._functions, classes, loops_ to construct new __Domain-bound series__ of functionality. This example illustrates the real world. 


```rust
fn add(a, b){ return a  + b; }
```
_Fig. 01, "Function definition in Rust"_

```rust
add(1 + 2);
```
_Fig. 02, "Function argument in Rust"_