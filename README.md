# README
This branch of the project is dedicated to Templates, Assets, Snippets, and Shared resources.

## Access
All files + directories in this branch can be accessed through multiple methods, depending on necessity.

The fasted use-case is using "git-checkout" from within another branch.
```sh
git checkout -b "share
```