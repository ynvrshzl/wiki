The `"README.md"` object is considered the key article, but is not solely a list of links (which can be auto-generated.)

For example, this is how [we structure top-level topics](./topics.md) ...e.g., `"physics/README.md"`

These articles should thread together the atomic objects cohesively, as [links](#), in article, readable narrative form.