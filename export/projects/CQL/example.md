```markdown
gpt, let's explore how we would actually use our codelens cql (code-query-language) project idea... remember? let's imagine this hypothetical scenario.

- open up our website working folder "~/web"
- markdown documentation lives alongside code now, becuase codelens can work as a standalone program, or inside a codeblock, rendered into html! similar to jupyter notebooks, but the notebook doesn't need to execute test code; it can document the code in real-time. like querying files in obsidian, inside codeblocks, which powers 90% of my personal daily-driver vault. anyways, the point is; a system that can introspect (query) is incredibly time-saving, analytically powerful, and practically applicable, if the analysis tools are applied to life correctly (this idea comes from data-science, only a few models are ever useful.)
- we query blocks of code where the word "todo" appears. rendered as a codemap. this can easily be done with vscode's "find in all files" feature. but the ability to query and work with the code itself, programatically, is kinda the meta-feature of this thing, and the thing that makes it most exciting! 
```