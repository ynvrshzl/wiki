# Managing Conflicts in Git
When managing conflicts in this __Git__ cloud system, we simply use the Command line, no panic included.

## Options
Git contains several commands for handling divergent branches (conflicts). Which is usually cuased by remote/local difference in the file system. E.g. editng files on a local vscode clone, and editing files on mobile git.

1. Git Pull - [Merge](merge.md) 
2. Git Rebase - [Rebase](rebase.md)